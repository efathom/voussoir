# ctxforge audit (2026-05)

Auto-generated by `scripts/ctxforge_audit.py`.


## Protocols

- `ctxforge.protocols.compactor.IContextAssembler` — methods: ['__init__', 'assemble', 'fit_to_budget']
- `ctxforge.protocols.context.IContextIndexer` — methods: ['__init__', 'index_item', 'remove_item', 'search']
- `ctxforge.protocols.context.IContextItem` — methods: ['__init__', 'to_prompt_format']
- `ctxforge.protocols.context.IContextReranker` — methods: ['__init__', 'rerank']
- `ctxforge.protocols.context.IContextRetriever` — methods: ['__init__', 'retrieve_items', 'retrieve_with_scores']
- `ctxforge.protocols.context.IContextStore` — methods: ['__init__', 'delete', 'get', 'save', 'search']
- `ctxforge.protocols.expertise.ICurator` — methods: ['__init__', 'curate']
- `ctxforge.protocols.expertise.IExpertiseReranker` — methods: ['__init__', 'rerank']
- `ctxforge.protocols.expertise.IExpertiseRetriever` — methods: ['__init__', 'retrieve']
- `ctxforge.protocols.expertise.IExpertiseStore` — methods: ['__init__', 'add_item', 'delete', 'get_item', 'get_items_by_section', 'get_usage_stats', 'list_expertise', 'load', 'log_usage', 'remove_item', 'save', 'search_items', 'update_item', 'update_item_counts']
- `ctxforge.protocols.expertise.IReflector` — methods: ['__init__', 'reflect']
- `ctxforge.protocols.extractor.IConsolidator` — methods: ['__init__', 'consolidate', 'find_duplicates', 'merge_memories']
- `ctxforge.protocols.extractor.IMemoryExtractor` — methods: ['__init__', 'extract', 'extract_from_text', 'validate_candidate']
- `ctxforge.protocols.graph.IGraphExtractor` — methods: ['__init__', 'extract']
- `ctxforge.protocols.graph.IGraphStore` — methods: ['__init__', 'add_episodes', 'delete_communities', 'delete_scope', 'get_communities_for_nodes', 'get_edges_by_ids', 'invalidate_edges', 'load_enhanced_index', 'save_enhanced_index', 'search', 'search_nodes_semantic', 'upsert_communities', 'upsert_edges', 'upsert_memberships', 'upsert_nodes']
- `ctxforge.protocols.graph_maintenance.IGraphContradictionDetector` — methods: ['__init__', 'detect_contradictions']
- `ctxforge.protocols.graph_maintenance.IGraphEdgeTemporalExtractor` — methods: ['__init__', 'extract_temporal_info']
- `ctxforge.protocols.lifecycle.IAsyncClosable` — methods: ['__init__', 'close']
- `ctxforge.protocols.lifecycle.IAsyncInitializable` — methods: ['__init__', 'initialize']
- `ctxforge.protocols.llm.IEmbeddingProvider` — methods: ['__init__', 'embed', 'embed_single']
- `ctxforge.protocols.llm.ILLMProvider` — methods: ['__init__', 'chat', 'count_message_tokens', 'count_tokens', 'generate', 'stream']
- `ctxforge.middleware.protocol.IMiddleware` — methods: ['__init__', 'process']
- `ctxforge.protocols.middleware_factory.IMiddlewareFactory` — methods: ['__init__', 'create']
- `ctxforge.protocols.retriever.IReranker` — methods: ['__init__', 'rerank']
- `ctxforge.protocols.retriever.IRetriever` — methods: ['__init__', 'retrieve', 'retrieve_by_embedding', 'retrieve_related']
- `ctxforge.protocols.scoped_memory.IScopedMemoryStore` — methods: ['__init__', 'clear', 'count', 'delete', 'delete_by_id', 'get', 'get_by_id', 'initialize', 'list_by_scope', 'query', 'save', 'update']
- `ctxforge.protocols.semantic_model.ISemanticModelStore` — methods: ['__init__', 'list_models', 'load', 'save']
- `ctxforge.protocols.skill.ISkillStore` — methods: ['__init__', 'clear', 'count', 'delete', 'get', 'get_all_relationships', 'get_metadata', 'get_relationships', 'initialize', 'list_all_metadata', 'list_metadata', 'save', 'save_relationships', 'search_by_category', 'search_by_tags', 'search_by_trigger', 'update_effectiveness']
- `ctxforge.protocols.storage.IMemoryStore` — methods: ['__init__', 'add', 'count', 'delete', 'get', 'get_by_user', 'keyword_search', 'search', 'update']
- `ctxforge.protocols.storage.ISessionStore` — methods: ['__init__', 'delete', 'exists', 'list_sessions', 'load', 'save']
- `ctxforge.protocols.tokenizer.ITokenizerProvider` — methods: ['__init__', 'count_message_tokens', 'count_tokens']
- `ctxforge.protocols.update_planner.IMemoryUpdatePlanner` — methods: ['__init__', 'plan']
- `ctxforge.vectorstores.protocol.IVectorStore` — methods: ['__init__', 'close', 'count', 'delete', 'delete_by_filter', 'delete_namespace', 'describe', 'fetch', 'initialize', 'list_namespaces', 'query', 'query_by_id', 'upsert']

## Services in `engine/services/`

- `assembly_service` — ⚠ not referenced in factory.py
- `code_analyzer` — ⚠ not referenced in factory.py
- `compaction_service` — ⚠ not referenced in factory.py
- `consolidation_service` — ⚠ not referenced in factory.py
- `context_delegator_service` — ⚠ not referenced in factory.py
- `context_health_service` — ⚠ not referenced in factory.py
- `context_window_service` — ⚠ not referenced in factory.py
- `document_skill_creator` — ⚠ not referenced in factory.py
- `expertise_service` — ⚠ not referenced in factory.py
- `expertise_snapshot_service` — ✓ used by factory
- `fusion_service` — ⚠ not referenced in factory.py
- `github_skill_creator` — ⚠ not referenced in factory.py
- `graph_service` — ✓ used by factory
- `headline_service` — ⚠ not referenced in factory.py
- `intent_note_service` — ✓ used by factory
- `memory_service` — ✓ used by factory
- `memory_synthesizer_service` — ✓ used by factory
- `memory_update_service` — ⚠ not referenced in factory.py
- `personalization_metrics_service` — ✓ used by factory
- `preference_evolution_service` — ✓ used by factory
- `query_rewriter_service` — ⚠ not referenced in factory.py
- `retrieval_controller_service` — ⚠ not referenced in factory.py
- `scoped_memory_service` — ✓ used by factory
- `script_runner` — ⚠ not referenced in factory.py
- `semantic_model_service` — ✓ used by factory
- `session_service` — ⚠ not referenced in factory.py
- `skill_content_loader` — ⚠ not referenced in factory.py
- `skill_effectiveness_service` — ⚠ not referenced in factory.py
- `skill_evaluation_service` — ⚠ not referenced in factory.py
- `skill_execution_service` — ⚠ not referenced in factory.py
- `skill_generator_service` — ⚠ not referenced in factory.py
- `skill_inheritance_service` — ⚠ not referenced in factory.py
- `skill_lifecycle_service` — ⚠ not referenced in factory.py
- `skill_matcher` — ✓ used by factory
- `skill_relationship_service` — ⚠ not referenced in factory.py
- `skill_service` — ✓ used by factory
- `skill_validator` — ⚠ not referenced in factory.py
- `sufficiency_service` — ⚠ not referenced in factory.py
- `timeline_service` — ⚠ not referenced in factory.py
- `tool_bridge` — ⚠ not referenced in factory.py
- `turn_recording_service` — ⚠ not referenced in factory.py
- `validated_knowledge_service` — ✓ used by factory

## EngineDeps `Optional[Any]` slots

- reflector: Optional[Any] = None
- curator: Optional[Any] = None
- scoped_memory_service: Optional[Any] = None
- skill_service: Optional[Any] = None
- graph_service: Optional[Any] = None

## Triage — voussoir Phase 0 conclusions

### Keep (depended on directly by voussoir)

These Protocols are re-exported from `voussoir.protocols` (Task 12) and bound in
`voussoir.container.defaults` (Task 11) or planned for Phase 1+ wiring:

- `ILLMProvider`, `IEmbeddingProvider` — default LLM provider wiring
- `IMemoryStore`, `ISessionStore` — Tier 0 memory + session in Phase 1
- `IRetriever`, `IVectorStore` — retrieval substrate (Phase 2)
- `ISkillStore` — skill discovery (Phase 2)
- `ITokenizerProvider` — token counting for budget-aware context assembly
- `IContextAssembler` — packs context windows
- `IExpertiseRetriever`, `IExpertiseStore` — expertise tier (Phase 2 yase)
- `IMemoryExtractor` — memory extraction (Phase 2)

Active services voussoir's Phase 1+ flows will lean on:
`memory_service`, `session_service` (despite the heuristic flag — it's referenced
elsewhere), `scoped_memory_service`, `skill_service`, `skill_matcher`,
`semantic_model_service`, `intent_note_service`, `validated_knowledge_service`.

### Defer (not needed for v1)

These exist in ctxforge but voussoir v1 has no requirement for them — revisit
if Phase 3+ delegation or Phase 4 A2A surfaces a need:

- Graph Protocols: `IGraphExtractor`, `IGraphStore`, `IGraphContradictionDetector`,
  `IGraphEdgeTemporalExtractor` (graph memory backend; v1.5+)
- `ISemanticModelStore`, `IMemoryUpdatePlanner` (advanced memory consolidation)
- `IConsolidator`, `IReflector`, `ICurator` (expertise pipeline beyond retrieval)
- `IMiddleware`, `IMiddlewareFactory` (ctxforge has its own middleware system —
  voussoir defines its own `Middleware` Protocol in Task 13; do NOT bridge)
- `IAsyncClosable`, `IAsyncInitializable` (lifecycle marker Protocols — voussoir
  manages its own lifecycle via the container)
- The 28 services flagged "not referenced in factory.py" — most are experimental
  or invoked outside the standard wiring path; don't depend on them blindly.

### Upstream PRs to send (light-refactor, per spec §8.4.8)

- [ ] **Add Anthropic provider to ctxforge.** Currently `ctxforge.llm.openai_provider`
  exists but no Anthropic counterpart, forcing voussoir to ship its own
  `voussoir.llm.anthropic` (Phase 1). Upstreaming closes the asymmetry. Open
  question whether ctxforge wants to own Anthropic at all — see
  `docs/superpowers/specs/...` discussion. (Also see Task 11 review notes.)
- [ ] **Tighten EngineDeps `Optional[Any]` slots.** The five fields below should
  be typed against their Protocol contracts so consumers can plug typed bindings:
    - `reflector: Optional[IReflector]`
    - `curator: Optional[ICurator]`
    - `scoped_memory_service: Optional[IScopedMemoryStore]`
    - `skill_service: Optional[ISkillService]` (or whatever the canonical contract is)
    - `graph_service: Optional[IGraphStore]` (if v1.5 graph memory lands)
- [ ] **Rename `IFoo` → `Foo` on Protocols** (cosmetic; non-breaking via re-export shim).
  Spec §8.4.8 calls for dropping the Hungarian-style `I` prefix. Defer until
  voussoir's protocol surface is locked in (post-Phase 1).
- [ ] **Confirm async surface consistency.** Most ctxforge Protocols expose
  `async def` methods, but a manual scan during Phase 1 should verify every
  Protocol method voussoir touches is async or has an async wrapper documented.
- [ ] **Document which config keys voussoir reads.** ctxforge's `EngineConfig`
  has many knobs; voussoir should publish the subset it relies on so future
  ctxforge config changes don't silently break voussoir.

### Audit script caveats

- The "active vs experimental" heuristic is naïve substring match in
  `factory.py` — cross-reference any deferral with grep across the rest of
  ctxforge before deleting anything. Several `⚠` flags above are likely false
  positives (referenced from `context_engine.py` or elsewhere).
- Method lists include `__init__` from the Protocol's MRO; ignore that when
  evaluating contract surface.
- `IMiddleware` lives at `ctxforge.middleware.protocol` (not `protocols/`), so
  it shows up under "Protocols" by virtue of the `_is_runtime_protocol` heuristic.
  This is correct — just noting the path inconsistency for the rename PR above.
